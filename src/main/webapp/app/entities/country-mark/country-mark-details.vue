<template>
  <div class="row justify-content-center">
    <div class="col-8">
      <div v-if="countryMark">
        <h2 class="jh-entity-heading" data-cy="countryMarkDetailsHeading">
          <span v-text="$t('gma2App.countryMark.detail.title')">CountryMark</span> {{ countryMark.id }}
        </h2>
        <dl class="row jh-entity-details">
          <dt>
            <span v-text="$t('gma2App.countryMark.relType')">Rel Type</span>
          </dt>
          <dd>
            <span>{{ countryMark.relType }}</span>
          </dd>
          <dt>
            <span v-text="$t('gma2App.countryMark.country')">Country</span>
          </dt>
          <dd>
            <div v-if="countryMark.country">
              <router-link :to="{ name: 'CountryView', params: { countryId: countryMark.country.id } }">{{
                countryMark.country.id
              }}</router-link>
            </div>
          </dd>
          <dt>
            <span v-text="$t('gma2App.countryMark.mark')">Mark</span>
          </dt>
          <dd>
            <div v-if="countryMark.mark">
              <router-link :to="{ name: 'MarkView', params: { markId: countryMark.mark.id } }">{{ countryMark.mark.id }}</router-link>
            </div>
          </dd>
        </dl>
        <button type="submit" v-on:click.prevent="previousState()" class="btn btn-info" data-cy="entityDetailsBackButton">
          <font-awesome-icon icon="arrow-left"></font-awesome-icon>&nbsp;<span v-text="$t('entity.action.back')"> Back</span>
        </button>
        <router-link
          v-if="countryMark.id"
          :to="{ name: 'CountryMarkEdit', params: { countryMarkId: countryMark.id } }"
          custom
          v-slot="{ navigate }"
        >
          <button @click="navigate" class="btn btn-primary">
            <font-awesome-icon icon="pencil-alt"></font-awesome-icon>&nbsp;<span v-text="$t('entity.action.edit')"> Edit</span>
          </button>
        </router-link>
      </div>
    </div>
  </div>
</template>

<script lang="ts" src="./country-mark-details.component.ts"></script>
